 <nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <ul class="nav navbar-nav">
      <li class="active"><a href="/">Home</a></li>
      <% if user_signed_in? %>
        <li><a href="/rsvp">RSVP</a></li>
	<li><a href="/musics">Music</a></li>
      <% end %>
    </ul>
    <ul class="nav navbar-nav navbar-right">
      <% if user_signed_in? %>
      <li><a>
        <%= current_user == nil ? "" :
            current_user.first_name != "" ? (current_user.first_name.humanize + " " + current_user.last_name.humanize) :
            current_user.username != ""? current_user.username.split.map(&:capitalize).join(' ') :
            "Welcome!"
           %>
         </a></li>
       <% end %>
      <% if !user_signed_in? %>
        <li><a id="login" href="#" data-toggle="modal" data-target="#login-modal"><span class="glyphicon glyphicon-log-in"></span>  Login</a></li>
      <% else %>
        <li><a rel="nofollow" data-method="delete" href="/users/sign_out"><span class="glyphicon glyphicon-log-in"></span>  Sign out</a></li>
      <% end %>
    </ul>
  </div>
</nav>
